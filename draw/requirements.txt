# draw/ — Spot Vicon Canvas Draw
# ================================
# Core web server (always required)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# Numerical math (always required)
numpy>=1.24.0

# ---- Optional: install only what you need ----

# Real Vicon DataStream SDK
# vicon-dssdk>=1.12.0

# Boston Dynamics Spot SDK (real robot mode)
# bosdyn-client>=4.0.0
# bosdyn-mission>=4.0.0
# bosdyn-api>=4.0.0
